fof-oauth:
  admin:
    settings:
      providers:
        auth0:
          description: Register your forum with the Auth0 Developer Portal {link}

          client_id_label: => fof-oauth.ref.settings.client_id
          client_secret_label: => fof-oauth.ref.settings.client_secret

  forum:
    log_in:
      with_auth0_button: '=> fof-oauth.forum.log_in.with_button'

    providers:
      auth0: '=> fof-oauth.lib.providers.auth0'

  lib:
    providers:
      auth0: Auth0
